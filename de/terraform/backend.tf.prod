#use s3 to store remote state
terraform {

  # bucket and dynamodb_table should be pre-created
  backend "s3" {
    bucket  = "com.fngn.prod.terraform.us-west-1"
    key     = "aws-infra-as-code/de/terraform/terraform.tfstate"
    region  = "us-west-1"
    encrypt = true

    dynamodb_table = "terraform-locks"
  }
}
